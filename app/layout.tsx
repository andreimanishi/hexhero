import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "HexHero",
  description:
    "Become a color expert with HexHero! A fun color-guessing game using HEX codes. Test and improve your color perception skills. Perfect for designers, color enthusiasts, and everyone in between!",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
